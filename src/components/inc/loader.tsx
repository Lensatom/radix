import { Text } from '../base'

const Loader = () => {
  return (
    <div className='w-full h-full flex justify-center items-center'>
      <Text className='font-bold text-primary-dark animate-pulse'>RADIX</Text>
    </div>
  )
}

export default Loader